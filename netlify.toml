[build]
  command = "CI=false npm run build"
  publish = "dist" # Change to "build" if you are using Create React App instead of Vite
  
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200